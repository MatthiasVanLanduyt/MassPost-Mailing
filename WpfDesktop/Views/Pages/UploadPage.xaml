<Page x:Class="WpfDesktop.Views.Pages.UploadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfDesktop.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      Title="UploadPage">

    <ScrollViewer>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <ui:TextBlock
                    Text="Address Upload"
                    FontSize="28"
                    FontTypography="Title"/>
                <ui:TextBlock
                    Text="Configure your upload settings and provide the required information below."
                    FontSize="14"
                    Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- File Upload Section -->
            <StackPanel Grid.Row="1" Margin="0,0,0,24" >

                <ui:Card>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                <ui:Button
                                    Margin="8,0,0,0"
                                    Appearance="Primary"
                                    DockPanel.Dock="Right"
                                    Content="Choose File"
                                    Icon="Upload24"
                                    Command="{Binding UploadCommand}"/>

                                <ui:Button
                                    Content="Process"
                                    Icon="ArrowUpload24"
                                    Appearance="Primary"
                                    Command="{Binding ProcessCommand}"
                                    Margin="8,0,8,0"/>
                                
                                <ui:Button
                                    Content="Cancel"
                                    Appearance="Secondary"
                                    Margin="0,0,8,0"
                                    Command="{Binding CancelCommand}"/>
                            </StackPanel>

                            
                            <ui:TextBox
                                Icon="Document24"
                                PlaceholderText="Select address file"
                                Text="{Binding SelectedFilePath, Mode=OneWay}"
                                IsReadOnly="True"/>
                        </DockPanel>

                        <ui:InfoBar
                            Grid.Row="1"
                            Title="Supported Formats"
                            Message="Please upload a CSV file with address records"
                            Severity="Informational"
                            IsOpen="True"/>
                    </Grid>
                </ui:Card>

               
            </StackPanel>

            <!-- Required Information (Mail Request) -->
            <ui:Card Grid.Row="2" Margin="0,0,0,16">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <ui:TextBlock
         Text="Required Information"
         FontSize="16"
         FontWeight="Medium"
         Margin="0,0,0,16"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Row 1 -->

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,12">
                                <ui:TextBlock Text="Account Id" Margin="0,0,0,4"/>
                                <ui:TextBox
             PlaceholderText="Account ID"
             Text="{Binding AccountID}"
             Icon="PersonCard24"/>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,12">
                                <ui:TextBlock Text="Expected Delivery Date" Margin="0,0,0,4"/>
                                <DatePicker
                                    SelectedDate="{Binding ExpectedDelivery}"/>
                            </StackPanel>

                            <!-- Row 2 -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,12">
                                <ui:TextBlock Text="Mail Format" Margin="0,0,0,4"/>
                                <ComboBox
                 SelectedItem="{Binding MailFormat}"
                 ItemsSource="{Binding MailFormatOptions}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,12">
                                <ui:TextBlock Text="Sequence Number" Margin="0,0,0,4"/>
                                <ui:TextBox

                                    PlaceholderText="Sequence Number"
                                    Text="{Binding SequenceNumber}"
                                    Icon="Number24"/>
                            </StackPanel>



                            <!-- Row 3 -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,12">
                                <ui:TextBlock Text="Deposit Type" Margin="0,0,0,4"/>
                                <ComboBox
                 SelectedItem="{Binding DepositType}"
                 ItemsSource="{Binding DepositTypeOptions}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="8,0,0,12">
                                <ui:TextBlock Text="Deposit Identifier" Margin="0,0,0,4"/>

                                <ui:TextBox
                 PlaceholderText="Deposit Identifier"
                 Text="{Binding DepositIdentifier}"
                 Icon="DocumentNumber24"/>
                            </StackPanel>

                            
                            
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        
                    </StackPanel>
                </Grid>
            </ui:Card>
            
        </Grid>
    </ScrollViewer>
</Page>
