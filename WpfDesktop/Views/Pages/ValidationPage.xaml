<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WpfDesktop.Views.Pages.ValidationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Address Validation"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <ui:TextBlock
                Text="Address Validation"
                FontSize="28"
                FontTypography="Title"/>
            <ui:TextBlock
                Text="Upload your XML file to validate address records"
                FontSize="14"
                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Upload Section -->
        <ui:Card Grid.Row="1" Margin="0,0,0,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,12">
                    <ui:Button
                        Margin="8,0,0,0"
                        Appearance="Primary"
                        DockPanel.Dock="Right"
                        Content="Upload"
                        Icon="Upload24"
                        Command="{Binding UploadCommand}"/>
                    <ui:TextBox
                        Icon="Document24"
                        PlaceholderText="Select XML file"
                        Text="{Binding SelectedFilePath, Mode=OneWay}"
                        IsReadOnly="True"/>
                </DockPanel>

                <ProgressBar
                    Grid.Row="1"
                    Value="{Binding ProcessingProgress}"
                    Maximum="100"
                    Margin="0,0,0,8"/>

                <ui:InfoBar
                    Grid.Row="2"
                    IsOpen="{Binding IsProcessing}"
                    Message="Processing XML file..."
                    Severity="Informational"
                    IsClosable="False"/>
            </Grid>
        </ui:Card>

        <!-- Validation Results -->
        <ScrollViewer Grid.Row="2">
            <StackPanel>
                <!-- Summary Statistics -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ui:Card Grid.Column="0" Margin="0,0,12,0">
                        <StackPanel>
                            <ui:TextBlock
                                Text="Total Records"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock
                                Text="{Binding TotalRecords}"
                                FontSize="32"
                                FontWeight="Medium"/>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Grid.Column="1" Margin="4,0,8,0">
                        <StackPanel>
                            <ui:TextBlock
                                Text="Valid Records"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock
                                Text="{Binding ValidRecords}"
                                FontSize="32"
                                FontWeight="Medium"
                                Foreground="#4CAF50"/>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Grid.Column="2" Margin="4,0,8,0">
                        <StackPanel>
                            <ui:TextBlock
                                Text="With Warnings"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock
                                Text="{Binding WarningRecords}"
                                FontSize="32"
                                FontWeight="Medium"
                                Foreground="#FFC107"/>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Grid.Column="3" Margin="12,0,0,0">
                        <StackPanel>
                            <ui:TextBlock
                                Text="With Errors"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock
                                Text="{Binding ErrorRecords}"
                                FontSize="32"
                                FontWeight="Medium"
                                Foreground="#F44336"/>
                        </StackPanel>
                    </ui:Card>
                </Grid>

                <!-- Compliance Rates -->
                <ui:Card Margin="0,0,0,24">
                    <StackPanel>
                        <ui:TextBlock
                            Text="Compliance Rates"
                            FontSize="16"
                            FontWeight="Medium"
                            Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>

                            <ui:TextBlock
                                Grid.Column="0"
                                Text="Address Compliance"
                                VerticalAlignment="Center"/>
                            <ProgressBar
                                Grid.Column="1"
                                Value="{Binding AddressComplianceRate}"
                                Maximum="100"
                                Margin="12,0"/>
                            <ui:TextBlock
                                Grid.Column="2"
                                Text="{Binding AddressComplianceRate, StringFormat={}{0}%}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>

                            <ui:TextBlock
                                Grid.Column="0"
                                Text="Building Compliance"
                                VerticalAlignment="Center"/>
                            <ProgressBar
                                Grid.Column="1"
                                Value="{Binding BuildingComplianceRate}"
                                Maximum="100"
                                Margin="12,0"/>
                            <ui:TextBlock
                                Grid.Column="2"
                                Text="{Binding BuildingComplianceRate, StringFormat={}{0}%}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>

                            <ui:TextBlock
                                Grid.Column="0"
                                Text="Presorting Compliance"
                                VerticalAlignment="Center"/>
                            <ProgressBar
                                Grid.Column="1"
                                Value="{Binding PresortingComplianceRate}"
                                Maximum="100"
                                Margin="12,0"/>
                            <ui:TextBlock
                                Grid.Column="2"
                                Text="{Binding PresortingComplianceRate, StringFormat={}{0}%}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <!-- Warning Details -->
                <ui:Card>
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ui:TextBlock
                                Grid.Column="0"
                                Text="Addresses with Warnings"
                                FontSize="16"
                                FontWeight="Medium"/>

                            <ui:Button
                                Grid.Column="1"
                                Content="View All"
                                Icon="ArrowRight24"
                                Command="{Binding ViewAllWarningsCommand}"/>
                        </Grid>

                        <ListView
                            ItemsSource="{Binding WarningAddresses}"
                            MaxHeight="200">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Address" Width="300">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Address}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Warning Type" Width="200">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding WarningType}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Severity" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Severity}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
