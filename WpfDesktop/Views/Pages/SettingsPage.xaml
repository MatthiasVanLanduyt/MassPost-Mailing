<Page x:Class="WpfDesktop.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfDesktop.Views.Pages"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{Binding Title}">

    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ui:InfoBar Message="Settings Page" Severity="Informational" />
        <!-- Optional Settings -->
        <Expander Grid.Row="1" Margin="0,0,0,16">
            <Expander.Header>
                <ui:TextBlock
             Text="File Generation Options"
             FontSize="16"
             FontWeight="Medium"/>
            </Expander.Header>
            <ui:Card Margin="0,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 1 -->
                    <ComboBox
                 Grid.Column="0"
                 Margin="0,0,8,12"
                 
                 SelectedItem="{Binding OutputFormat}"
                 ItemsSource="{Binding OutputFormatOptions}"/>

                    <ComboBox
                 Grid.Column="1"
                 Margin="8,0,8,12"
                
                 SelectedItem="{Binding Mode}"
                 ItemsSource="{Binding ModeOptions}"/>

                    <ComboBox
                 Grid.Column="2"
                 Margin="8,0,0,12"
                 
                 SelectedItem="{Binding SortingMode}"
                 ItemsSource="{Binding SortingModeOptions}"/>

                    <!-- Row 2 -->
                    <ui:ToggleSwitch
                 Grid.Row="1" Grid.Column="0"
                 Margin="0,0,8,0"
                 Content="Generate MailIds"
                 IsChecked="{Binding GenerateMailIds}"/>

                    <ui:ToggleSwitch
                 Grid.Row="1" Grid.Column="1"
                 Margin="8,0,0,0"
                 Content="Generate PreSorting"
                 IsChecked="{Binding GeneratePreSorting}"/>
                </Grid>
            </ui:Card>
        </Expander>
        <ui:Button Content="Save" Grid.Row="2" Grid.Column="0" Command="{Binding SaveCommand}" Appearance="Primary" HorizontalAlignment="Center"/>

        
    </Grid>
</Page>
